#![allow(unused)]

fn main() {
    // Variables
    //  - Immutable by default
    //  - Use 'mut' keyword to make it mutable
    let mut x = 1;
    x += 1;
    // Type inference
    // "let x: i32" == "let x"
    // Expressing the type of variable explicitly is adding "let x: i32" in it.
}
